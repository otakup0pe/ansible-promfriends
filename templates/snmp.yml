---
edgerouter:
  version: 2
  auth:
    community: "{{promsnmp_v2_community}}"
  walk:
{% for walk in promsnmp_walk %}
{'    '}- {{walk}}
{% endfor %}
  metrics:
{% for oid in promsnmp_oid %}
{'    '}- {{oid | to_nice_yaml(indent=4,sort_keys=False) | indent(4, False) }}
{% endfor %}
