---
dependencies:
  - role: "ANXS.python"
    python_uv_install: true
    when: promnode_pkg_exporter | default(true)
galaxy_info:
  namespace: otakup0pe
  role_name: promfriends
  author: Jonathan Freedman
  license: MIT
  galaxy_tags:
    - prometheus
    - metrics
    - docker
  min_ansible_version: 2.0
  description: "Install and run Prometheus and Friends"
  platforms:
    - name: Ubuntu
      versions:
        - all
